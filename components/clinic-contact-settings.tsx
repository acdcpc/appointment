import { useState } from "react";
import { Pressable, StyleSheet, Text, TextInput, View } from "react-native";
import { useColors } from "@/hooks/use-colors";
import { usePediatricCare } from "@/lib/pediatric-care";

export function ClinicContactSettings() { const colors = useColors(); const { clinicLocation, updateClinicLocation } = usePediatricCare(); const [address, setAddress] = useState(clinicLocation.address); const [mapUrl, setMapUrl] = useState(clinicLocation.mapUrl); const [message, setMessage] = useState(""); const save = () => { if (!address.trim() || !/^https?:\/\//.test(mapUrl.trim())) { setMessage("Enter a clinic address and a complete https map link."); return; } updateClinicLocation({ address: address.trim(), mapUrl: mapUrl.trim(), isProvisional: false }); setMessage("Final clinic address saved for the current workspace."); }; return <View style={[s.card, { backgroundColor: colors.surface, borderColor: colors.border }]}><Text style={[s.title, { color: colors.foreground }]}>Clinic address administration</Text><Text style={[s.note, { color: colors.muted }]}>Replace the provisional address with the final clinic location when confirmed.</Text><TextInput value={address} onChangeText={setAddress} placeholder="Final clinic address" placeholderTextColor={colors.muted} multiline style={[s.input, { color: colors.foreground, borderColor: colors.border }]} /><TextInput value={mapUrl} onChangeText={setMapUrl} placeholder="https://maps…" placeholderTextColor={colors.muted} autoCapitalize="none" style={[s.input, { color: colors.foreground, borderColor: colors.border }]} /><Pressable onPress={save} style={[s.button, { backgroundColor: colors.primary }]}><Text style={s.buttonText}>Save final address</Text></Pressable>{message ? <Text style={{ color: message.includes("saved") ? colors.success : colors.error, fontSize: 12, fontWeight: "700" }}>{message}</Text> : null}</View>; }
const s = StyleSheet.create({ card: { borderWidth: 1, borderRadius: 16, padding: 15, gap: 9, marginTop: 18 }, title: { fontSize: 16, fontWeight: "800" }, note: { fontSize: 12, lineHeight: 18 }, input: { borderWidth: 1, borderRadius: 10, padding: 11, minHeight: 44, fontSize: 13 }, button: { borderRadius: 11, padding: 13, alignItems: "center" }, buttonText: { color: "#FFFFFF", fontWeight: "800" } });
