import { StyleSheet, Text, View } from "react-native";

import { useColors } from "@/hooks/use-colors";
import { usePediatricCare } from "@/lib/pediatric-care";

export function ClinicDayFocus() {
  const colors = useColors();
  const { appointments, auditEvents, earlierSlotRequests, children } = usePediatricCare();
  const activeVisits = appointments.filter((appointment) => appointment.status !== "cancelled" && appointment.status !== "completed");
  const needsIntake = activeVisits.filter((appointment) => appointment.status === "needs-intake");
  const guardianConfirmed = activeVisits.filter((appointment) => Boolean(appointment.guardianConfirmedAt));
  const appointmentChanges = auditEvents.filter((event) => event.type === "appointment-change").slice(0, 3);
  const pendingWaitlistResponses = earlierSlotRequests.filter((request) => request.status === "responded" && request.offer?.parentResponse && !request.offer.clinicianAcknowledgedAt);
  const nextVisit = activeVisits[0];
  return <View style={[styles.card, { backgroundColor: colors.surface, borderColor: colors.border }]}><Text style={[styles.title, { color: colors.foreground }]}>Clinic day focus</Text><Text style={[styles.note, { color: colors.muted }]}>A protected, at-a-glance handoff inspired by leading patient portals: check readiness before opening detailed records.</Text><View style={styles.metrics}><View style={[styles.metric, { backgroundColor: "#E0F2F3" }]}><Text style={[styles.value, { color: colors.primary }]}>{activeVisits.length}</Text><Text style={[styles.label, { color: colors.muted }]}>Active visits</Text></View><View style={[styles.metric, { backgroundColor: needsIntake.length ? "#FFF3CD" : "#EAF7F0" }]}><Text style={[styles.value, { color: needsIntake.length ? colors.warning : colors.success }]}>{needsIntake.length}</Text><Text style={[styles.label, { color: colors.muted }]}>Need intake</Text></View><View style={[styles.metric, { backgroundColor: appointmentChanges.length ? "#FDE7E2" : "#EAF7F0" }]}><Text style={[styles.value, { color: appointmentChanges.length ? colors.error : colors.success }]}>{appointmentChanges.length}</Text><Text style={[styles.label, { color: colors.muted }]}>Recent changes</Text></View></View>{pendingWaitlistResponses.length ? <View style={[styles.waitlistAlert, { backgroundColor: "#FFF8EB", borderColor: colors.warning }]}><Text style={[styles.nextLabel, { color: colors.warning }]}>WAITLIST RESPONSE IN TODAY’S REVIEW</Text>{pendingWaitlistResponses.map((request) => { const child = children.find((item) => item.id === request.childId); return <Text key={request.id} style={[styles.note, { color: colors.foreground }]}>{child?.name ?? "Child"} {request.offer?.parentResponse} {request.offer?.date} · {request.offer?.time}. Review the offer before changing the schedule.</Text>; })}</View> : null}<View style={[styles.attendance, { backgroundColor: guardianConfirmed.length ? "#EAF7F0" : "#FFF3CD", borderColor: guardianConfirmed.length ? colors.success : colors.warning }]}><Text style={[styles.attendanceValue, { color: guardianConfirmed.length ? colors.success : colors.warning }]}>{guardianConfirmed.length}</Text><Text style={[styles.note, { color: colors.muted }]}>{guardianConfirmed.length === 1 ? "guardian has confirmed an upcoming appointment" : "guardians have confirmed upcoming appointments"}</Text></View>{nextVisit ? <View style={[styles.next, { borderColor: colors.border }]}><Text style={[styles.nextLabel, { color: colors.muted }]}>NEXT SCHEDULED VISIT</Text><Text style={[styles.nextTitle, { color: colors.foreground }]}>{nextVisit.date} · {nextVisit.time}</Text><Text style={[styles.note, { color: nextVisit.guardianConfirmedAt ? colors.success : colors.muted }]}>{nextVisit.guardianConfirmedAt ? "Guardian attendance confirmed" : nextVisit.status === "needs-intake" ? "Confirm intake before visit" : "Guardian attendance not yet confirmed"}</Text></View> : <Text style={[styles.note, { color: colors.success }]}>No active appointments are awaiting review.</Text>}</View>;
}

const styles = StyleSheet.create({ card: { borderWidth: 1, borderRadius: 16, padding: 14, gap: 10, marginTop: 16 }, title: { fontSize: 18, fontWeight: "800" }, note: { fontSize: 12, lineHeight: 18 }, metrics: { flexDirection: "row", gap: 8 }, metric: { flex: 1, borderRadius: 12, padding: 10, gap: 3 }, value: { fontSize: 22, fontWeight: "800" }, label: { fontSize: 11, lineHeight: 15 }, waitlistAlert: { borderWidth: 1, borderRadius: 11, padding: 10, gap: 5 }, attendance: { borderWidth: 1, borderRadius: 11, padding: 10, flexDirection: "row", gap: 8, alignItems: "center" }, attendanceValue: { fontSize: 19, fontWeight: "800" }, next: { borderWidth: 1, borderRadius: 11, padding: 11, gap: 3 }, nextLabel: { fontSize: 10, fontWeight: "800", letterSpacing: 0.6 }, nextTitle: { fontSize: 14, fontWeight: "800" } });
